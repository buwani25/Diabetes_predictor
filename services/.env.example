# Database Configuration (PostgreSQL)
# Replace with your PostgreSQL connection details
database_url=postgresql://postgres:Buu#2002@localhost:5432/fdm_diabetes_db

# JWT Configuration
# Generate a strong random secret key for production
# You can use: openssl rand -hex 32
secret_key=your-super-secret-key-change-this-in-production
algorithm=HS256
access_token_expire_minutes=30

# CORS Configuration (comma-separated string)
# Add your frontend URLs
cors_origins_str=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173
